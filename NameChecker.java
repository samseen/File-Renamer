public class NameChecker {
	/* Keep the names of all the files in the target Folder in an array(it should be well formatted)
	* formatted means: (.) removed, trimmed, whitespaces in strings reduced to one
	*  Have an array for the names(formatted) of the second source folder
	*  Loop through the destination array with each source filename checking if it's in the array
	*  Keep the filename in an array if it's not in the destination folder.
	*/

	/* Classes Needed are: NameFormatter(To format the Strings of each file name)
	*  Create a class called FileCastle that has the names of the source and destination in a list of arrays
	*  BattleGround: Where the source castle are been iterated over the DestinationCastle
	*/
	
}